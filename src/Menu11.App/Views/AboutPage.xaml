<Page
    x:Class="Menu11.App.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer
        HorizontalContentAlignment="Stretch"
        HorizontalScrollMode="Disabled"
        HorizontalScrollBarVisibility="Disabled">
        <StackPanel
            Padding="28,22,28,32"
            HorizontalAlignment="Stretch"
            Spacing="12">
            <Image
                Width="72"
                Height="72"
                HorizontalAlignment="Left"
                Source="ms-appx:///Assets/AppIcon.png" />
            <StackPanel Spacing="4">
                <TextBlock x:Uid="AboutProductName" Style="{StaticResource PageTitleStyle}" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind ViewModel.ProductVersionLabel, Mode=OneWay}" />
            </StackPanel>

            <TextBlock
                MaxWidth="560"
                HorizontalAlignment="Left"
                FontSize="14"
                x:Uid="AboutDescription"
                TextWrapping="Wrap" />

            <Border Style="{StaticResource SettingRowStyle}">
                <TextBlock
                    x:Uid="AboutDisclaimer"
                    TextWrapping="Wrap" />
            </Border>

            <TextBlock x:Uid="UpdatesSection" Style="{StaticResource SectionTitleStyle}" />
            <Border Style="{StaticResource SettingTileStyle}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Spacing="2">
                        <TextBlock x:Uid="AutomaticUpdatesLabel" FontWeight="SemiBold" />
                        <TextBlock
                            x:Uid="AutomaticUpdatesDescription"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <ToggleSwitch
                        x:Name="AutomaticUpdatesToggle"
                        Grid.Column="1"
                        x:Uid="AutomaticUpdatesToggle"
                        IsOn="{x:Bind ViewModel.AutomaticallyCheckForUpdates, Mode=TwoWay}"
                        Style="{StaticResource CompactToggleSwitchStyle}" />
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button
                    x:Name="GitHubButton"
                    x:Uid="GitHubButton"
                    Click="GitHub_Click"
                    IsEnabled="{x:Bind ViewModel.HasRepositoryUrl, Mode=OneWay}"
                    Style="{StaticResource IconTextButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Viewbox Style="{StaticResource ButtonIconViewboxStyle}">
                            <SymbolIcon Symbol="Link" />
                        </Viewbox>
                        <TextBlock x:Uid="GitHubButtonLabel" Style="{StaticResource ButtonLabelStyle}" />
                    </StackPanel>
                </Button>
                <Button Click="License_Click" Style="{StaticResource IconTextButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Viewbox Style="{StaticResource ButtonIconViewboxStyle}">
                            <SymbolIcon Symbol="Document" />
                        </Viewbox>
                        <TextBlock x:Uid="LicenseButtonLabel" Style="{StaticResource ButtonLabelStyle}" />
                    </StackPanel>
                </Button>
                <Button
                    x:Name="UpdatesButton"
                    x:Uid="UpdatesButton"
                    Click="CheckForUpdates_Click"
                    IsEnabled="{x:Bind ViewModel.HasRepositoryUrl, Mode=OneWay}"
                    Style="{StaticResource IconTextButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Viewbox Style="{StaticResource ButtonIconViewboxStyle}">
                            <SymbolIcon Symbol="Sync" />
                        </Viewbox>
                        <TextBlock x:Uid="UpdatesButtonLabel" Style="{StaticResource ButtonLabelStyle}" />
                    </StackPanel>
                </Button>
            </StackPanel>

    <InfoBar
                x:Name="ActionResultBar"
                IsClosable="True"
                IsOpen="False" />
        </StackPanel>
    </ScrollViewer>
</Page>
